# 🎨 PyQt Painter с автосохранением - Лабораторная работа 4

## 📝 Что это вообще такое?

Короче говоря, это такое простенькое приложение для рисования на Python. Типа Paint, но на PyQt5 + QML. Главное фишка - автосохранение каждые 30 секунд. Рисуешь, и оно само сохраняет, чтобы ничего не потерять.

## 🎯 Что получается в итоге?

Приложение с таким функционалом:
- 🎨 Рисуешь на белом холсте (Canvas)
- 🎯 Выбираешь цвет (5 вариантов: голубой, зелёный, жёлтый, красный, сиреневый)
- 📏 Выбираешь толщину кисти (от 1 до 5 пикселей)
- 💾 Автосохранение каждые 30 секунд (всегда в один файл `autosave_backup.png`)
- 📂 Ручное сохранение (создаёт разные файлы с временной меткой)
- 🧹 Очистка холста одной кнопкой
- 📁 Выбор папки для сохранения
- 📊 Статус панель, которая показывает что происходит

## 📂 Структура файлов

```
lab_4/
├── main_complete.py          ← Главный backend (Python)
├── mainWindow.qml            ← Интерфейс (QML)
├── Circle.qml                ← Шаблон для кнопок толщины
├── Square.qml                ← Шаблон для кнопок цвета
└── readme.md                 ← Этот файл
```


## 💡 Как это работает?

### Фронт-энд (QML)
Окно приложения написано на QML. Там:
- Canvas - область для рисования
- Кнопки для выбора цвета и толщины
- Кнопки для сохранения, очистки, выбора папки

### Бэк-энд (Python)
Логика на Python (PyQt5):
- Таймер каждые 30 сек отправляет сигнал на сохранение
- Проверяет существование файла перед открытием
- Если файл нет - создаёт пустое белое изображение
- Сохраняет с метаданными (время создания, размер и т.д.)

## 🔑 Главное нюансы

### ✅ Автосохранение (smart)
- Каждые 30 секунд таймер срабатывает
- Сохраняется в `autosave_backup.png` (всегда ОДИН файл)
- Файл перезаписывается → не засоряется папка

### ✅ Ручное сохранение
- Когда нажимаешь "Сохранить сейчас"
- Создаёт новый файл: `drawing_YYYYMMDD_HHMMSS.png`
- Каждый раз новое имя → можешь сравнивать версии

### ✅ Проверка файла
В коде есть проверка `os.path.exists()`, потому что QML создаёт только путь, а не сам файл. Если файла нет → Python создаёт пустой белый PNG и сохраняет его.

## 📊 Скриншоты и что там происходит

### Скриншот 1: Главное окно приложения

<img width="1305" height="805" alt="SCR-20251207-uinn" src="https://github.com/user-attachments/assets/08a3d14f-ff1d-47da-b92c-aeefdcb011c8" />

**Что здесь видно:**
- Сверху указана папка для сохранения: `/Users/vsevolodkrecko/Documents/Python/PyQt_labs/saved_drawings`
- В середине холст с рисунком (кружка, сердце, котик)
- Левая сторона - выбор цвета (голубой, зелёный, жёлтый, красный, фиолетовый)
- Правая сторона - выбор толщины кисти (1-5)
- Внизу кнопки: "Сохранить сейчас", "Очистить холст", "Выбрать папку", "Открыть папку"
- Статус внизу: "Готов к работе" (всё работает)

Справа видно окно файлового браузера - это когда юзер кликнул "Открыть папку", и система показала все сохранённые файлы. Видно `autosave_backup.png` - это файл автосохранения, который перезаписывается.

---

### Скриншот 2: Диалог выбора папки

<img width="1304" height="810" alt="SCR-20251207-uhbh" src="https://github.com/user-attachments/assets/33263599-ab6c-4156-bec9-c0e1d353f456" />

**Что здесь видно:**
- Открылся стандартный диалог выбора папки (macOS)
- Папка `saved_drawings` уже выбрана (синяя)
- Внутри только 2 файла:
  - `autosave_backup_meta.json` (метаданные)
  - `autosave_backup.png` (сам файл автосохранения)
- Это означает, что автосохранение работает правильно - один файл, который перезаписывается

Раньше здесь было бы 100+ файлов, но мы сделали умное автосохранение, поэтому теперь только один файл.

---

### Скриншот 3: Логи в терминале

<img width="955" height="266" alt="SCR-20251207-uexo" src="https://github.com/user-attachments/assets/3958c82b-577a-4797-b011-ea8f7bf1ab27" />

**Что здесь видно (с низу вверх):**

```
qml: Запрос на автосохранение
qml: Подготовлено сохранение холста в: /var/folders/m_.../T/canvas_auto_2025-12-07T19-23-31-094Z.png
Автосохранение успешно: /Users/.../saved_drawings/drawing_20251207_232331.png
```

Это логи, когда программа работает. Каждые 30 секунд:
1. QML отправляет сигнал `saveRequest`
2. Готовит временный файл в `/tmp`
3. Отправляет в Python бэкенд
4. Python сохраняет в реальную папку
5. Выводит "Автосохранение успешно"

Дальше вверх видно такие же процессы - это значит, что автосохранение срабатывает регулярно, как надо.

## 🐛 Если что-то не работает

### "Файл не существует"
Это была ошибка, которую мы исправили. Теперь в коде проверка:
```python
if os.path.exists(image_path):
    image = QImage(image_path)
else:
    image = QImage(1024, 768, QImage.Format_ARGB32)
    image.fill(Qt.white)
```

### Много файлов в папке
Если видишь 100+ файлов - это потому что используешь старую версию кода. Обновись на новый `main_complete.py`, там автосохранение перезаписывается.

### Приложение не открывается
```bash
# Проверь, что QML файл на месте
ls ~/Documents/Python/PyQt_labs/lab_4/mainWindow.qml

# Проверь виртуальное окружение
source .venv/bin/activate

# Проверь, что PyQt5 установлён
pip install PyQt5
```

## 🎨 Фишки кода

### 1. Два типа сохранения
```python
def _save_canvas_from_image(self, image_path, is_autosave=False):
    if is_autosave:
        filepath = self.autosave_path  # autosave_backup.png
    else:
        filename = self._get_timestamp_filename()  # drawing_YYYYMMDD_HHMMSS.png
```

### 2. Таймер на Python
```python
self.timer = QTimer(self)
self.timer.timeout.connect(self._on_timer)
self.timer.start(self._interval_sec * 1000)  # 30 секунд
```

### 3. Сигналы PyQt5
```python
saveRequest = pyqtSignal()      # Автосохранение каждые 30 сек
saveCompleted = pyqtSignal(str) # Успешное сохранение
saveError = pyqtSignal(str)     # Ошибка сохранения
```

### 4. Шаблоны QML
Есть `Circle_template.qml` и `Square_template.qml` для красивых кнопок с анимацией. Можешь их использовать, чтобы сделать интерфейс ещё красивее.

## 🧪 Что я бы улучшил (если был бы больше времени)

- [ ] Добавить undo/redo (отмена/повтор)
- [ ] Поддержка загрузки существующих рисунков
- [ ] Разные инструменты (кисть, резинка, заливка)
- [ ] Палитра цветов вместо 5 фиксированных
- [ ] Экспорт в разные форматы (JPG, BMP и т.д.)
- [ ] Слои (layers)
- [ ] Линейка и направляющие

## 📚 Использованные технологии

- **Python 3.x** - язык программирования
- **PyQt5** - фреймворк для создания GUI
- **QML** - язык для описания интерфейса
- **Qt Canvas** - элемент для рисования
- **JSON** - для сохранения метаданных

## 🎓 Что я выучил, делая эту лабу

1. Как работает PyQt5 и QML вместе
2. Как создавать сигналы и слоты (signals & slots)
3. Как работает таймер в Qt
4. Как обрабатывать файлы на Python
5. Как проверять существование файлов и их обработку
6. Как работает автосохранение (не создавать 100+ файлов)

## 📝 Выводы

Приложение работает! ✅ Главное, что:
- Рисуешь на холсте
- Каждые 30 сек автоматически сохраняется (в один файл)
- Можно сохранить вручную (создаст отдельный файл)
- Ничего не потеряется
- Папка не засоряется сотнями файлов

Всё просто и работает.

---

**Создана:** 2025-12-07  
**Статус:** ✅ Работает нормально 
**Версия:** 1.0 (с перезаписью автосохранения)
